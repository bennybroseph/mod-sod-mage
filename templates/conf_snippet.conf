#
# TEMPLATE — paste into conf/mod_sod_mage.conf.dist, fill the placeholders. Not
# loaded from templates/ (only conf/*.conf.dist is). Read it in C++ with
#   sConfigMgr->GetOption<T>("SodMage.<Name>", <default>);
# Ship every key with its documented default; the module is a no-op when disabled.
#

#
#    SodMage.<Feature>.<Name>
#        Description: <What it controls, in one or two lines.>
#        Default:     <DEFAULT>
#

SodMage.<Feature>.<Name> = <DEFAULT>
